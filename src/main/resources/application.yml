spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fundoonotes
    username: root
    password: 12345678

  redis:
    host: localhost
    port: 6379

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080